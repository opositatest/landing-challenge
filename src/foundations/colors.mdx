import { Meta } from '@storybook/addon-docs/blocks';
import { referenceColorTokens } from './tokens-data';

<Meta title="Foundations/Colors" parameters={{ viewport: { disable: true } }} />

<div className="style-guide-stories colors-stories">
  <header>
    <h1>Colors</h1>
    <p>{'Shared reference color tokens used as the visual foundation of the system. These values are intended to be consumed through system tokens when defining final UI semantics.'}</p>
  </header>

  <section>
    <h2 id="reference-palette">Reference palette</h2>

    <div className="docs-token-grid">
    {referenceColorTokens.map(([label, _var, hex, oklch]) => (
      <div key={label} className="docs-token-card">
        <div className="docs-token-surface" style={{ backgroundColor: hex }} />

        <div className="docs-token-card-caption">
          <div className="color-title">{label}</div>
          <div className="docs-token-code color-hex ">{hex}</div>
          <div className="docs-token-code color-oklch">{oklch}</div>
        </div>
      </div>
    ))}
    </div>

  </section>
</div>
