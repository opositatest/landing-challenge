import { Meta } from '@storybook/addon-docs/blocks';
import { fontFamily, fontSizes, fontWeights, letterSpacing, lineHeights, previewText, lineHeightPreviewText } from './tokens-data';

<Meta title="Foundations/Typography" parameters={{ viewport: { disable: true } }} />

<div className="style-guide-stories typography-stories">
  <header>
    <h1>Typography</h1>
    <p>
      {
        'Shared reference typography tokens used as the visual foundation of the system. These values are intended to be consumed through system or component tokens when defining final typography styles.'
      }
    </p>
  </header>

<section>
  <h2 id="type-scale">Type scale</h2>
  <div className="docs-token-stack type-scale-stack">
    {Object.entries(fontSizes).map(([key, value]) => (
      <div key={key} className="docs-token-item">
        <div className="docs-token-preview type-scale-preview" style={{ fontSize: `var(${key})` }}>
          {previewText}
        </div>
        <div className="docs-token-label">{key}</div>
        <div className="docs-token-value">{value}</div>
      </div>
    ))}
  </div>
</section>

<section>
  <h2 id="font-family">Font family</h2>
  <ul className="docs-token-stack">
    {Object.entries(fontFamily).map(([key, value]) => (
      <li key={key} className="docs-token-item">
        <div className="docs-token-preview" style={{ fontFamily: value }}>
          {previewText}
        </div>
        <div className="docs-token-label">{key}</div>
        <div className="docs-token-value">{value}</div>
      </li>
    ))}
  </ul>
</section>

<section>
  <h2 id="font-weights">Font weights</h2>
  <ul className="docs-token-stack">
    {Object.entries(fontWeights).map(([key, value]) => (
      <li key={key} className="docs-token-item">
        <div className="docs-token-preview" style={{ fontWeight: value }}>
          {previewText}
        </div>
        <div className="docs-token-label">{key}</div>
        <div className="docs-token-value">{value}</div>
      </li>
    ))}
  </ul>
</section>

  <section>
    <h2 id="line-heights">Line heights</h2>
    <div className="docs-token-grid line-height-palette">
      {Object.entries(lineHeights).map(([key, [rawValue, cssValue]]) => (
        <div key={key} className="docs-token-card">
          <div className="docs-token-surface line-height-preview" style={{ lineHeight: cssValue }}>
            {lineHeightPreviewText}
          </div>
          <div className="docs-token-card-caption">
            <div className="docs-token-label">{key}</div>
            <div className="docs-token-value">{rawValue} · unitless</div>
          </div>
        </div>
      ))}
    </div>

  </section>

  <section>
    <h2 id="letter-spacing">Letter spacing</h2>
    <ul className="docs-token-stack">
      {Object.entries(letterSpacing).map(([key, value]) => (
        <li key={key} className="docs-token-item">
          <div className="docs-token-preview letter-spacing-preview" style={{ letterSpacing: value }}>
            {previewText}
          </div>
            <div className="docs-token-label">{key}</div>
            <div className="docs-token-value">{value}</div>
        </li>
      ))}
    </ul>
  </section>
</div>
